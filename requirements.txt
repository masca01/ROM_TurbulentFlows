# The third-party packages the code imports, as minimum versions. The code was verified
# with these on the Mac (Python 3.14, MPS/CPU) and with newer ones in the cloud
# (Python 3.11, CPU). Install with:  python3 -m pip install -r requirements.txt
numpy>=2.4
scipy>=1.17
torch>=2.10
h5py>=3.15
matplotlib>=3.10
openpyxl>=3.1
# tests only
pytest>=8
